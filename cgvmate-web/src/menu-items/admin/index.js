import admin from './admin';
import auth from './auth';
// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [auth, admin]
};

export default menuItems;
